name: kiemtra_firebase_27_12
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter

  # Giao diện iOS
  cupertino_icons: ^1.0.8

  # --- CÁC THƯ VIỆN ĐÃ ĐƯỢC HẠ PHIÊN BẢN ĐỂ KHỚP VỚI SDK 3.10.4 ---
  
  # Bộ Firebase tương thích (Khoảng giữa năm 2023)
  firebase_core: ^4.3.0
  firebase_auth: ^6.1.3
  cloud_firestore: ^6.1.1
  
  # Các tiện ích khác
  provider: ^6.0.5
  intl: ^0.20.2            # Bản 0.20 yêu cầu SDK mới hơn
  shared_preferences: ^2.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Linter bản 6.0 yêu cầu Dart SDK 3.5+, phải hạ xuống bản 2.0 cho SDK 3.10
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true